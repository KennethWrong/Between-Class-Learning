class mixer:
    def __init__(self, images):
        self.images = images
    
    def image_augmentation(image):
        #Used to augment the image using one of the papers given
        return

    def mix_images():
        #get ratio and mix the images and replace the images with mixed images
        #and set self.labels as the ratio of the mixed images
        return

    def mix_images_bc_plus():
        #Better mixing method
        return
    
    def mix_images_bc():
        #First between class mixing method proposed
        return
    
    def mix_images_basic():
        #simple equation for mixing
        # y = rx1 + (1- r)x2
        return
